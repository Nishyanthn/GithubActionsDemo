# GithubActionsDemo

Sandbox repo for learning how GitHub Actions can enforce an Azure Boards
work-item reference (`AB#1234`) in PR titles, commits, and branch names —
scoped to the `devtest` branch only.

## Sample change
This line was added to test the PR title check.

Testing branch name check with an intentionally wrong branch name.
